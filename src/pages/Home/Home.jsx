import React from "react";
import Alert from "../../components/Alert/Alert";

const Home = ({ }) => {
    return (
        <Alert text={"שלום שלום לדף-Home"}/>
    );
};

export default Home;